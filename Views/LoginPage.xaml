<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:BusTrackerApp.ViewModels"
             x:Class="BusTrackerApp.Views.LoginPage"
             x:DataType="viewmodels:LoginViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource Primary}">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">
            
            <!-- Logo y Título -->
            <Label Text="🚌" FontSize="80" HorizontalOptions="Center"/>
            <Label Text="Bus Tracker" 
                   FontSize="32" 
                   FontAttributes="Bold"
                   TextColor="White"
                   HorizontalOptions="Center"/>
            <Label Text="Sistema de Rastreo Universitario" 
                   FontSize="16"
                   TextColor="White"
                   Opacity="0.8"
                   HorizontalOptions="Center"
                   Margin="0,0,0,40"/>

            <!-- Formulario de Login -->
            <Border StrokeThickness="0" 
                    BackgroundColor="White"
                    Padding="20"
                    StrokeShape="RoundRectangle 15">
                <VerticalStackLayout Spacing="15">
                    
                    <Label Text="Iniciar Sesión" 
                           FontSize="24" 
                           FontAttributes="Bold"
                           TextColor="{StaticResource Primary}"/>

                    <Entry Placeholder="Email" 
                           Text="{Binding Email}"
                           Keyboard="Email"
                           ClearButtonVisibility="WhileEditing"/>

                    <Entry Placeholder="Contraseña" 
                           Text="{Binding Password}"
                           IsPassword="True"
                           ClearButtonVisibility="WhileEditing"/>

                    <Label Text="{Binding ErrorMessage}" 
                           TextColor="Red"
                           IsVisible="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyConverter}}"
                           FontSize="12"/>

                    <Button Text="Ingresar" 
                            Command="{Binding LoginCommand}"
                            IsEnabled="{Binding IsBusy, Converter={StaticResource InvertedBoolConverter}}"
                            BackgroundColor="{StaticResource Primary}"
                            TextColor="White"
                            HeightRequest="50"
                            CornerRadius="10"
                            Margin="0,10,0,0"/>

                    <ActivityIndicator IsRunning="{Binding IsBusy}" 
                                     IsVisible="{Binding IsBusy}"
                                     Color="{StaticResource Primary}"/>

                </VerticalStackLayout>
            </Border>

            <!-- Instrucciones de prueba -->
            <Border StrokeThickness="1" 
                    Stroke="White"
                    Opacity="0.7"
                    BackgroundColor="Transparent"
                    Padding="15"
                    StrokeShape="RoundRectangle 10"
                    Margin="0,20,0,0">
                <VerticalStackLayout Spacing="8">
                    <Label Text="💡 Modo Demo" 
                           FontSize="14" 
                           FontAttributes="Bold"
                           TextColor="White"/>
                    <Label FontSize="12" TextColor="White">
                        <Label.FormattedText>
                            <FormattedString>
                                <Span Text="• Pasajero: cualquier email sin 'conductor'"/>
                                <Span Text="&#x0a;"/>
                                <Span Text="• Conductor: email con 'conductor' o 'driver'"/>
                            </FormattedString>
                        </Label.FormattedText>
                    </Label>
                </VerticalStackLayout>
            </Border>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
